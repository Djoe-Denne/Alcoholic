package com.djden.alcoholic.application.progression;

/**
 * Presentation column. Machines and process types stay drink-agnostic;
 * only the quest graph names a lineage.
 */
public enum ProgressionLine {
    WINE,
    BEER,
    SHARED
}
