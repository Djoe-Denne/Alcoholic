.gradle/
.env
build/
**/build/
**/run/
**/run-*/
.idea/
*.iml
*.ipr
*.iws
*.class
.classpath
.project
.settings/
**/bin/
out/
logs/
crash-reports/
.grepai/

# Local Cursor MCP (absolute Node / project paths). Shared skills stay tracked.
.cursor/mcp.json

# OS / JVM noise
.DS_Store
Thumbs.db
Desktop.ini
nul
NUL
*~
*.bak
*.orig
*.swp
*.swo
hs_err_pid*
replay_pid*
*.hprof
.kotlin/
.tmp-review/*

# Forge HashCache: rewritten on every runData, not a source of truth.
# Keep generated JSON/PNG tracked; drop only the hash sidecar folder.
**/src/generated/resources/.cache/

# Python bytecode
**/__pycache__/
*.py[cod]

# Local art locks / checksums (not a repo gate yet)
SHA256SUMS.txt
face_lock.json
*.bbmodelBackup
*.bbmodel.backup
